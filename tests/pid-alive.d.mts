/** Type surface for `pid-alive.mjs` — plain JS on purpose (see the module's own comment). */
export declare function pidState(pid: number): string | null;
export declare function isLiveHolder(pid: number): boolean;
